---
description: Enter Terniq planning mode explicitly. Routes into terniq-think for pre-implementation design judgment, evaluation, and scope pressure-testing.
---

# /terniq:plan

Owning skill: `terniq-think`

Purpose:

- force explicit entry into planning and evaluation mode
- bypass latent matching uncertainty when the user wants a direct workflow start

Rule:

- route into `terniq-think`
- do not maintain separate planning logic here

Typical uses:

- plan this feature before implementation
- decide whether this workflow should exist
- pressure-test an architecture decision

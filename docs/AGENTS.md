# docs/
> L2 | Parent: /Volumes/new-room/code-web-app/terniq/AGENTS.md

Members
2026-04-25-v1-design.md: Terniq launch design specification defining the product position, skill matrix, plugin structure, and implementation phases.

Architecture Decisions
`docs/` contains high-signal design and planning material only. It should not mix in execution scripts or implementation detail; `skills/`, `agents/`, and `commands/` define the real structure, while docs explain why the layers are shaped this way.

Development Rules
When adding a design draft or release note, keep the date-prefixed naming convention, update this member list first, and then verify whether the parent map also needs a sync.

Change Log
2026-04-25: Created the Terniq docs directory map and registered the first design specification.

[PROTOCOL]: 变更时更新此头部，然后检查 AGENTS.md
